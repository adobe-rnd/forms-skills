{
  "name": "@aemforms/forms-skills-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for the aem-forms skillpack (validate)",
  "scripts": {
    "validate": "find skills/ -name SKILL.md -not -path '*/.venv/*' -exec dirname {} \\; | xargs -I {} skills-ref validate {}"
  },
  "devDependencies": {
    "skills-ref": "^0.1.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/adobe/skills.git"
  },
  "license": "Apache-2.0"
}
