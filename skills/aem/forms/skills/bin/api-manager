#!/usr/bin/env bash
set -e
PLUGIN_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
source "$PLUGIN_ROOT/bin/_resolve-workspace"
cd "$FORMS_WORKSPACE"
exec "$PLUGIN_ROOT/bin/python3" "$PLUGIN_ROOT/scripts/api_manager/cli.py" "$@"
