#!/usr/bin/env bash
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PLUGIN_ROOT="$(cd "$SCRIPT_DIR/../../../../.." && pwd)"
source "$PLUGIN_ROOT/bin/_resolve-workspace"
cd "$FORMS_WORKSPACE"
export PYTHONPATH="$SCRIPT_DIR:$PYTHONPATH"
exec "$PLUGIN_ROOT/bin/python3" -m cct.cli "$@"
